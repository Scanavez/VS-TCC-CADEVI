@model IEnumerable<CadeviTCC.Models.DTO.DocumentoDTO>

@{
    ViewBag.Title = "IndexDoc";
}

<h2>Documentos</h2>

<p>
    @Html.ActionLink("Vincular Documento", "VincularDocumento")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Desvincular", "Desvincular", new { id = item.IdDocumento }) |
            @Html.ActionLink("Buscar", "Buscar", new { id=item.IdDocumento }) 
        </td>
    </tr>
}

</table>
